# 🚀 Super Starter Markup (Fixed)
Pug + SCSS + esbuild + BrowserSync + SVG-sprite + Sharp. Guards for missing folders, ESM-safe imports.

## Commands
npm i
npm run dev
npm run build
REV=1 npm run build
npm run img:build
npm run img:watch
