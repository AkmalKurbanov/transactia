mixin imgFixed(name, w, h, folder = "images/decors")
  picture
    source(srcset=`/transactia/dist/${folder}/${name}.avif` type="image/avif")
    source(srcset=`/transactia/dist/images/${folder}/${name}.webp` type="image/webp")
    img(
      src=`/transactia/dist/${folder}/${name}.png`
      width=w
      height=h
      alt=""
      loading="lazy"
      decoding="async"
    )
//- +imgFixed("visa", 48, 48, "images/ui/icons")    